spring:
  application:
    name: 01-sb-apache-kafka-producer
  datasource:
    url: jdbc:mysql://localhost:3306/student
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect


  devtools:
    restart:
      enabled: false

#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://localhost:8763/eureka/
#    fetch-registry: true
#management:
#  endpoints:
#    web:
#      exposure:
#        include: '*'
